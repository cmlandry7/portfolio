= form_for([@project, @project.images.build]) do |f|
  .field
    = f.label :caption
    = f.text_field :caption
  .field
    = f.label :description
    = f.text_field :description
  .actions
    = f.submit